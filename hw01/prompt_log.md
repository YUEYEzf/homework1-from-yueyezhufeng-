# AI 交互日志
## 2026-03-11
### 1. 需求描述
我需要完成《人工智能导论》课程作业 01：
1. 初始化标准 Python 工程，包含 `src/` 源码目录和 `tests/` 测试目录
2. 用回溯算法实现八皇后问题（8-Queens）的求解器
3. 编写单元测试，验证算法在 N=4、N=8 时的解数量正确
4. 故意在代码中引入一个小 Bug，观察并记录 AI 如何根据错误日志定位并修复

### 2. AI 生成代码
- 生成 `src/eight_queens.py`：实现 `EightQueens` 类，包含 `is_safe`（安全检查）、`backtrack`（回溯递归）、`solve`（启动求解）等核心方法
- 生成 `tests/test_eight_queens.py`：编写 3 个测试用例，分别验证 4 皇后解数、8 皇后解数、解的合法性

### 3. Bug 引入与处理
1. **引入 Bug**：修改 `is_safe` 方法，将斜线判断 `abs(row - i) == abs(col - self.board[i])` 改为 `abs(row - i) == (col - self.board[i])`，故意去掉第二个 `abs()`，导致逻辑错误
2. **发现 Bug**：运行单元测试，`test_n4_solution_count` 失败，4 皇后问题的解数量变为 0，与预期 2 个解不符
3. **定位 Bug**：通过测试报错信息和代码逻辑分析，发现斜线判断缺少绝对值，导致负差值被误判为冲突，无法找到合法解
4. **修复 Bug**：恢复 `abs(col - self.board[i])`，重新运行测试，所有用例通过，解数量恢复正常

### 4. 最终验证
- 4 皇后问题解数量：2（符合预期）
- 8 皇后问题解数量：92（符合预期）
- 所有单元测试通过，解的合法性验证完成